# Archive — stale documentation

Everything in this folder is stale: superseded, rejected, cursed, or describing
systems that no longer exist. Record only — nothing here is citable as a source
of truth. The standing canon is `TECH_DESIGN_BIBLE/` (repo root); standing
planning and daily records live in `docs/planning/`.
